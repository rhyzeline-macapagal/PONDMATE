<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F9F9F9">

    <!-- Page Title -->
    <TextView
        android:id="@+id/titleFeeds"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Feeds Prices"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center"
        android:background="#2196F3"
        android:textColor="@android:color/white" />

    <!-- Subtitle / Description -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:text="Below are the current feed prices per breed and feed type. Tap 'Edit' to update."
        android:textSize="14sp"
        android:textColor="#444"
        android:gravity="center"/>

    <!-- Table Scroll -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp">

        <TableLayout
            android:id="@+id/tableFeeds"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:stretchColumns="0,1,2,3,4"
            android:background="@android:color/white"
            android:padding="8dp"
            android:elevation="2dp">

            <!-- Header -->
            <TableRow
                android:background="#EEEEEE"
                android:padding="8dp">

                <TextView
                    android:text="Breed"
                    android:padding="4dp"
                    android:textStyle="bold"
                    android:textColor="#000" />

                <TextView
                    android:text="Feed Type"
                    android:padding="4dp"
                    android:textStyle="bold"
                    android:textColor="#000" />

                <TextView
                    android:text="Price/kg"
                    android:padding="4dp"
                    android:textStyle="bold"
                    android:textColor="#000" />

                <TextView
                    android:text="Date Updated"
                    android:padding="4dp"
                    android:textStyle="bold"
                    android:textColor="#000" />

                <TextView
                    android:text="Action"
                    android:padding="4dp"
                    android:textStyle="bold"
                    android:textColor="#000" />
            </TableRow>

            <!-- Dynamic rows will be inserted via Java -->
        </TableLayout>
    </ScrollView>
</LinearLayout>
